#ifndef TRACKING_ALGORITHM_H
#define TRACKING_ALGORITHM_H
/*
#include <Godot.hpp>
#include <Node2D.hpp>

class TrackingAlgorithm : public godot::Node2D {
    GODOT_CLASS(TrackingAlgorithm, godot::Node2D)

public:
    static void _register_methods();
    void _init();
    void _process(float delta);

    // Método que ejecuta el algoritmo de búsqueda de caminos
    godot::Array find_path(godot::Vector2 start, godot::Vector2 end);
};
*/
#endif // TRACKING_ALGORITHM_H